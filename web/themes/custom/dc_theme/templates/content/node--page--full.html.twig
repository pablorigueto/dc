<basicpage>
 
   <div class="basicpage_full_title">
    {{ content.field_title_to_display['#items'].0.value }}
  </div>

  <div class="basicpage_full_title">
    {{ img_title }}
  </div>

  <div class="basicpage_full_image">
    {% set imageAlt = content.field_title_to_display['#items'].0.value %}
     <img src="{{ file_url(thumbnail['#uri']) }}" alt="{{ imageAlt }}">
  </div>

  <div class="basicpage_full_body">
    {{ content.field_content_body }}
  </div>

  <div class="basicpage_full_node_owner_or_changed">
    {{ node_owner_or_changed }}
  </div>

    {# <a class="frontpage-results-img" href="{{ url }}">
      
      <div class="frontpage-result-thumbnail">

      <div class="frontpage-label-main">
        <div class="frontpage-label {{ node_type }}">
          {{ node_type|upper }}
        </div>
      </div>

        <div class="frontpage-title-display">

          <div class="frontpage-title">
            {{ title_display }}
          </div>
          <div class="profile-frontpage">

            <div class="profile-img-name">
              <img class="profile-frontpage-img" src="{{ file_url(image_profile) }}" alt="User Profile Image">
              <div class="owner-frontpage">
                {{ node_owner_or_changed }}
              </div>
            </div>
    
            {{ create_or_changed }}
    
          </div>

        </div>
        {{ thumbnail }}
      </div>
    </a> #}
 
</basicpage>
